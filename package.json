{
  "name": "blognew-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/build-posts.cjs && vite",
    "build": "node scripts/build-posts.cjs && vite build",
    "build:posts": "node scripts/build-posts.cjs",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.39.2",
    "vite": "^8.0.12",
    "wrangler": "^4.97.0"
  }
}
